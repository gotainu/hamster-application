{
  "name": "hamster-switchbot-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "commonjs",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.6.3"
  }
}